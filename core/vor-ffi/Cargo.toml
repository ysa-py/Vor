[package]
name = "vor-ffi"
description = "Stable C ABI surface for the VOR Flutter clients (JSON in / JSON out)"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
serde_json = { workspace = true }
vor-license = { path = "../vor-license" }
vor-engine = { path = "../vor-engine" }
